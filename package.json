{
  "name": "blnk-store",
  "version": "1.0.0",
  "private": true,
  "description": "BLNK Store mobile app",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "android:build": "npx cap add android && npx cap copy android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
